import React from 'react';
import {View, Text} from 'react-native';

export const Home = () => (
  <View style={{alignItems: 'center', justifyContent: 'center', flex: 1}}>
    <Text style={{fontSize: 30, fontFamily: 'bold'}}>Welcome to Home page</Text>
  </View>
);
